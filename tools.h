#include <stdio.h>

//ascii pair to hex value
unsigned char hexConvert(char, char);

//read file into the lower order values of the main memory, returns the first address that's free after the program code.
int readFile(FILE*, unsigned char*);

